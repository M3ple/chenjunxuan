$(function () {

  /*$("#btn_login").click(function () {
    //拿到上面文本框中的内容
    var username = $("#username").val();
    var pwd = $("#password").val();
    $("#myForm").submit(function (e) {
      console.log(e);
    });*/

  /*$.ajax({
    type: "POST",
    url: "/login",
    dataType: "json",
    contentType: 'application/json',
    cache: false,
    data: JSON.stringify({
      username: username,
      password: pwd
    }),
    success: function (responseData) {
      if (200 == responseData.code) {
        alert("登陆成功!");
        window.location.href = "http://localhost:8080/list.jsp";
      } else {
        alert("用户名或密码错误");
      }
    },
    error: function (data) {
      console.log(data);
    }
  });*/
  /*});*/
});